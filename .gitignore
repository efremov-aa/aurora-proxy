# Данные (свои ключи, статусы, блеклист, секреты) — НЕ в репозиторий.
data/
*.log
__pycache__/
*.pyc
.env
*.zip
xray
xray.json
*.tmp
.DS_Store

windows/installer/output/
windows/nssm/nssm.exe
*.bak_mojibake
AUDIT_FIX_TRACKER.md

# Мастер-ключ шифрования и его lock (создаются в корне при запуске вне data/).
.aurora_key
.aurora_key.lock
*.aurora_key

# Артефакты тестов прав доступа / хранилищ.
store.json
perm-target.json
perm-dir/
*.perm-*.json

# А-161: архивы браузерного расширения (PRO-фича) - их раздаёт панель.
# A-164: архивы браузерного расширения раздаёт только головной сервер,
# в публичной ветке их нет.
assets/

# A-163: папка с тестами остаётся только локально (решение владельца:
# на сайте GitHub она убрана). Локально гоняется через run_local_tests.py.
tests/
